/** @type {import('tailwindcss').Config} */

module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // paths to all of your components
  ],
  theme: {
    extend: {},
  },
  plugins: [],
  // Optionally, you can enable JIT mode by adding the `mode` property:
  // mode: 'jit',
}